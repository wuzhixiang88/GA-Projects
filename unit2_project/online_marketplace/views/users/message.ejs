<%- include("../partials/header.ejs") %>
<div class="container">
    <div class="row bg-secondary p-3">
        <div class="container-fluid col-12 d-flex">
            <div class="me-auto text-white fs-4 ms-2">
                Message
            </div>
            <div class="d-flex justify-content-end align-items-center">
                <a 
                    class="btn btn-secondary text-center fw-bold" 
                    role="button"
                    href="/user/inbox"
                >
                    Back  
                </a>
            </div>
        </div>    
    </div>

    <div class="row bg-light">
        <div class="container border-bottom">
            <div class="mb-3 ms-4">
                <% if (conversation.userOne === username) { %>
                    <%= conversation.userTwo %> 
                <% } else { %>
                    <%= conversation.userOne %>
                <% }; %>
            </div>

            <% for (let i = 0; i < conversation.messages.length; i++) { %>
                <% if (conversation.messages[i].username !== username) { %>
                    <p class="d-flex justify-content-start ms-4">
                        <%= conversation.messages[i].body %>
                    </p>
                <% } else { %>
                    <p class="d-flex justify-content-end me-3">
                        <%= conversation.messages[i].body %>
                    </p>
                <% }; %>
            <% }; %>
        </div>
        <div class="container-fluid border-bottom">
            <form
                method="POST"
                action="/user/inbox/<%= conversation._id %>/?_method=PATCH"
                class="mb-0"
            >
                <div class="col-md-12 ms-1 p-3">
                    <div class="input-group mb-0">
                        <input 
                            type="text" 
                            class="form-control" 
                            name="message"
                            placeholder="Type here..." 
                        >
                        <button 
                            class="btn btn-outline-secondary" 
                            type="submit" 
                        >
                            Send
                        </button>
                        <input
                            type="hidden"
                            name="messageId"
                            value="<%= conversation._id %>"
                        >
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>
<%- include("../partials/footer.ejs") %>